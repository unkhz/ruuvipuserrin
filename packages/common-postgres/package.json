{
  "name": "@ruuvipuserrin/common-postgres",
  "version": "0.0.0",
  "main": "dist/index.js",
  "module": "index.ts",
  "type": "commonjs",
  "scripts": {
    "prettier-write": "prettier --write *.json *.ts lib/**/*.ts",
    "prettier-check": "prettier --check *.json *.ts lib/**/*.ts",
    "test-unit": "jest --no-cache --ci",
    "lint": "npm run prettier-check",
    "test": "npm run test-unit",
    "node:build": "tsc --project tsconfig.build.json",
    "migrate": "ts-node ./lib/migrate.ts"
  },
  "devDependencies": {
    "@ruuvipuserrin/base-node": "*",
    "@types/pg": "^8.6.6"
  },
  "dependencies": {
    "kysely": "^0.23.4",
    "pg": "^8.9.0",
    "zod": "^3.20.2"
  }
}
